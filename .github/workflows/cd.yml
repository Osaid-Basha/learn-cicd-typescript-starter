name: cd

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_CREDENTIALS }}

      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: qwiklabs-gcp-04-96c7117ddfbf

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy notely \
            --image europe-docker.pkg.dev/qwiklabs-gcp-04-96c7117ddfbf/notely-ar-repo/notely:v2 \
            --region us-west1 \
            --allow-unauthenticated \
            --project qwiklabs-gcp-04-96c7117ddfbf \
            --max-instances=4